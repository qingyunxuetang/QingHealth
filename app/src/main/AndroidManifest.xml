<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.qingyunxuetang.qinghealth">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme" >
        <activity android:name=".ui.WelcomeActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
        <activity android:name=".ui.news.NewsListActivity"/>
        <activity android:name=".ui.news.NewsListActivity2"/>
        <activity android:name=".ui.news.NewsListActivity3"/>
        <activity android:name=".ui.news.NewsDetailActivity"/>
        <activity android:name=".ui.demo.JSONDemoActivity"/>
        <activity android:name=".ui.main.MainActivity"/>
    </application>
</manifest>
